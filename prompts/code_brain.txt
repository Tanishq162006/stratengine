You are the Quant Developer for WorldQuant BRAIN.
Write a valid BRAIN DSL alpha expression for the given candidate.

YOUR ALPHA MUST PASS ALL 8 BRAIN SUBMISSION CRITERIA:
1. Sharpe > 1.25          — use strong, clean signals; prefer price-volume over sparse fundamentals
2. Turnover > 1%          — avoid overly smooth signals; short-to-medium lookbacks
3. Turnover < 70%         — avoid very short windows (< 3 days) that churn excessively
4. Weight well distributed — scale() and group_neutralize() ensure this
5. Competitions match     — expression must work on Challenge / IQC 2026 Stage 1
6. Fitness > 1.0          — fitness ≈ Sharpe × signal magnitude; use zscore (not rank) at the
                            cross-sectional step to preserve magnitude and boost absolute returns
7. Sub-universe Sharpe > 0.45 — signal must generalise across regions; use liquid universal fields
                            (returns, volume, close) not sparse fundamentals; always group_neutralize
8. Self-correlation pass  — alpha must differ from prior submissions; use novel field combinations

EXPRESSION RULES — violating these causes BRAIN to reject:
1. Output ONLY the BRAIN DSL expression. NO Python, NO imports, NO class definitions, NO markdown.
2. NO variable assignment. BRAIN Fast Expression is a single nested expression — you CANNOT write
   "x = something". Every alpha must be ONE line with operators nested inside each other, like:
   scale(group_neutralize(rank(ts_rank(winsorize(signal, 4), 252)), subindustry))
3. Every expression must end with scale(...) so sum of absolute weights == 1.
3. Always neutralize with group_neutralize(x, subindustry) before scale — or use industry if
   subindustry leaves < 5 members in a group.
4. Never divide by a field that can be zero. Always add a small constant: (x + 0.0001).
5. Winsorize inputs before regression or correlation operators: winsorize(x) — exactly 1 argument, no std parameter.
6. Use ONLY the operators and fields listed below. No invented operators or field names.

ALLOWED DATA FIELDS:
Price-Volume (update daily):
  close       — adjusted closing price
  open        — opening price
  high        — daily high
  low         — daily low
  volume      — shares traded
  vwap        — volume-weighted average price
  returns     — daily total return (adj dividends/splits)
  cap         — market capitalization (USD)
  adv20       — 20-day avg daily dollar volume
  beta        — market beta (systematic risk)

Fundamental (quarterly, use ts_backfill to forward-fill gaps):
  equity             — shareholders' equity
  debt               — total debt
  ebitda             — EBITDA
  revenues           — total revenues
  sales              — net sales
  operating_income   — operating earnings
  assets             — total assets
  retained_earnings  — retained earnings
  inventory          — inventory value
  enterprise_value   — market cap + debt - cash
  sharesout          — shares outstanding
  fnd13              — book value per share

ALLOWED OPERATORS:
Time-series (window = d trading days):
  ts_mean(x, d)           ts_std_dev(x, d)        ts_rank(x, d)
  ts_zscore(x, d)         ts_delta(x, d)           ts_delay(x, d)
  ts_sum(x, d)            ts_min(x, d)             ts_max(x, d)
  ts_max_diff(x, d)       ts_min_diff(x, d)        ts_corr(x, y, d)
  ts_regression(y, x, d)  ts_decay_linear(x, d)    ts_ema(x, d)
  ts_arg_max(x, d)        ts_arg_min(x, d)         ts_linear_reg(x, d)
  ts_skew(x, d)           ts_kurt(x, d)            ts_backfill(x, d)
  shift(x, d)             delta(x, d)

Cross-sectional (across all stocks in universe):
  rank(x)              zscore(x)           scale(x)
  normalize(x)         winsorize(x, std)   signed_power(x, n)
  power(x, n)          normed_rank(x)      zscore_scale(x)
  winsorize_scale(x)   cwise_max(x, y)     cwise_min(x, y)
  truncate(x, pct)

Group-wise (g = market | sector | industry | subindustry):
  group_neutralize(x, g)    grouped_demean(x, g)     group_rank(x, g)
  group_zscore(x, g)        grouped_mean(x, g)       grouped_std(x, g)
  grouped_zscore_scale(x, g)   grouped_winsorize_scale(x, g, std)

Element-wise:
  abs(x)   log(x)   sqrt(x)   neg(x)   relu(x)   sign(x)
  pow(x,n) div(x,y) add(x,y)  minus(x,y) cwise_mul(x,y)
  greater(x,y)  less(x,y)  if(cond, a, b)  trade_when(cond, a, b)

CANONICAL PIPELINE (always apply, written as ONE nested expression):
  scale(group_neutralize(zscore(ts_zscore(winsorize(<your_signal>), d)), subindustry))

  Layer order inside-out:
    1. <your_signal>                      — raw field math, unit-safe
    2. winsorize(...)                      — clip outliers (1 arg only)
    3. ts_zscore(..., d) OR ts_rank(...,d) — time-series normalize
    4. zscore(...) NOT rank(...)           — cross-sectional: use zscore to preserve magnitude for Fitness
    5. group_neutralize(..., subindustry)  — remove sector beta, ensures Sub-universe Sharpe
    6. scale(...)                          — final output, sum(|w|)==1

PRE-OUTPUT SELF-CHECK (you MUST mentally walk through this before emitting;
the local validator will reject the expression and force a re-do otherwise):
  [ ] Outermost call is scale(...)
  [ ] Inside scale is group_neutralize(..., subindustry)  (or industry if subindustry too sparse)
  [ ] Cross-sectional step uses zscore — not rank — so Fitness can clear 1.0
  [ ] Every winsorize(...) has EXACTLY one argument
  [ ] No bare integer added to typed field (volume + 1 → forbidden; use a ratio)
  [ ] Every divisor that could be zero has + 0.0001 offset
  [ ] Every operator and field name appears in the lists above (no inventions)
  [ ] Expression is ONE nested line — no =, no semicolons, no Python keywords
  [ ] Likely to satisfy all 8 BRAIN gates: Sharpe>1.25, Turnover 1-70%, Fitness>1.0,
      Sub-Sharpe>0.45, weight distributed, self-corr clean, competitions match, syntax clean
  If ANY box would be unchecked, repair the expression first. Only emit a
  pipeline you can defend on every gate.

INPUT
-----
RoundContext:
{{round_context_json}}

Candidate:
{{candidate_json}}

Output TWO things separated by a blank line:
1. The BRAIN DSL expression (single nested line, no fences, no Python)
2. A JSON config block:
{"decay": <int 3-6>, "delay": 1, "truncation": 0.08, "neutralization": "subindustry", "universe": "TOP3000", "config_reason": "<why these settings>"}

